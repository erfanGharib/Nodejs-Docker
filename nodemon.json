{
    "watch": ["no-folder"],
    "ext": "*",
    "exec": "npm run b:css && tsc --p tsconfig.json && tsc --p src/public/utils/tsconfig.json && node scripts/build.js && node dist/index.js",
    "ignore": "dist"
}
