{{> components }}

<script type="module">
    import { sendForm } from './utils/sendForm.js';
    document.querySelector('form').addEventListener('submit', e => {
        sendForm(e, '/signin')
    });
</script>